{
  "name": "futuremeds-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/futuremeds-map.js --outdir dist --minify --target browser --format esm --external '*maplibre-gl-shared.mjs' && cp node_modules/maplibre-gl/dist/maplibre-gl-shared.mjs node_modules/maplibre-gl/dist/maplibre-gl-worker.mjs dist/",
    "dev": "bun run build && bun scripts/dev.ts",
    "seed:webflow": "bun scripts/from-webflow.ts"
  },
  "dependencies": {
    "maplibre-gl": "^6.9.0",
    "papaparse": "^5.7.0"
  }
}
